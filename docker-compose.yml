services:
  backend:
    build: 
      context: rg.fr-par.scw.cloud/atut-group-2/backend
      dockerfile: Dockerfile
    container_name: backend  # Nom du conteneur
    ports:
      - "8080:8050"
    restart: always

  frontend:
    build:
      context: rg.fr-par.scw.cloud/atut-group-2/frontend
      dockerfile: Dockerfile
    container_name: frontend
    ports: 
      - "8081:8051"
    depends_on:
      - backend
    restart: always